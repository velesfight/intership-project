---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nude-web
  namespace: dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nude-web
  template:
    metadata:
      labels:
        app: nude-web
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: nude-web
          image: timeofprogramming/nude_web:__TAG__
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: nude-web
  namespace: dev
spec:
  selector:
    app: nude-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nude-web-ingress
  namespace: dev
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  tls:
    - hosts:
        - dev.app.nude.web.ghfls.ru
      secretName: dev-app-nude-web-ghfls-ru-tls
  rules:
    - host: dev.app.nude.web.ghfls.ru
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nude-web
                port:
                  number: 80
